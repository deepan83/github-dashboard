{{#bs-navbar class="navbar navbar-inverse"}}
  <div class="navbar-header">
    <a class="navbar-brand" href="#"><p class="homeBtn">GitHub Fascia</p></a>
    {{#bs-navbar-toggle type="primary"}}
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    {{/bs-navbar-toggle}}
  </div>
    {{#if session.isAuthenticated}}
      {{#bs-navbar-content}}
        <p class="navbar-text">{{session.user.login}}</p>
        {{#bs-navbar-nav}}
          {{#bs-nav-item}}{{#link-to "pull-requests"}}<p class="button">Pull requests</p>{{/link-to}}{{/bs-nav-item}}
          {{#bs-nav-item}}{{#link-to "releases"}}<p class="button">Releases</p>{{/link-to}}{{/bs-nav-item}}
        {{/bs-navbar-nav}}
      {{/bs-navbar-content}}
    {{/if}}
{{/bs-navbar}}
